<Window x:Class="PLCConfigFileGenerator.TreeNodeEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PLCConfigFileGenerator"
        mc:Ignorable="d"
        Title="TreeNodeWindow" Height="130" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label x:Name="Label" Margin="10"/>
        <TextBox x:Name="TextBox" Grid.Column="1" Margin="10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Grid.ColumnSpan="2">
            <Button Width="60" Margin="10" Click="Button_Click">OK</Button>
            <Button Width="60" Margin="10" Click="Button_Click">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
